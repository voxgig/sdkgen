{
  "_comment": [
    "EMIT the shipped scaffold components (project/.sdk/src/cmp/**) into the",
    "staging consumer built by build/scaffold-stage.js, so test/generate.test.ts",
    "can actually RUN them — requirePath does a real Node require against",
    "<folder>/.sdk/dist/cmp/<lang>/. Compiler options mirror",
    "tsconfig.scaffold.json (which stays noEmit and remains the type-check",
    "gate); only the output settings differ. Nothing here is published: the",
    "npm `files` list ships project/, not dist-test-scaffold/."
  ],
  "compilerOptions": {
    "target": "ES2021",
    "module": "node16",
    "moduleResolution": "node16",
    "strict": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "sourceMap": true,
    "rootDir": "./project/.sdk/src/cmp",
    "outDir": "./dist-test-scaffold/.sdk/dist/cmp",
    "paths": {
      "@voxgig/sdkgen": [
        "./dist/sdkgen.d.ts"
      ]
    }
  },
  "include": [
    "project/.sdk/src/cmp/**/*.ts"
  ],
  "exclude": [
    "project/.sdk/src/cmp/**/fragment/**"
  ]
}
