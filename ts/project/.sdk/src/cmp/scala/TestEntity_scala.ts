
import { cmp } from '@voxgig/sdkgen'


// Placeholder: the API-specific entity flow test generator. The generated
// entity CRUD flow is currently exercised by the template SdkFlowTest and the
// struct corpus; a model-driven per-entity generator will be added here.
const TestEntity = cmp(function TestEntity(_props: any) {
})


export {
  TestEntity
}
