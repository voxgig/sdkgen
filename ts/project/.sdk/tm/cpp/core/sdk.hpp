// ProjectName SDK — umbrella include. A test or consumer includes this one
// header to get the whole SDK: the runtime type graph, the pipeline
// utilities, the vendored struct, the generated API config, the per-entity
// clients and the generated client class.

#ifndef SDK_SDK_HPP
#define SDK_SDK_HPP

#include "types.hpp"
#include "../utility/pipeline.hpp"
#include "../feature/base.hpp"
#include "config.hpp"
#include "../entity/entities.hpp"
#include "client.hpp"

#endif // SDK_SDK_HPP
