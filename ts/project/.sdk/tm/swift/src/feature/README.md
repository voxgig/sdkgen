# Feature extension folders

Each subfolder is a copy-target for `voxgig-sdkgen feature add <name>`: adding a
feature copies its folder's files into the generated SDK. These folders are
placeholders (the Swift feature implementations live in
`Sources/ProjectNameSDK/feature/`).
