
main: kit: target: c: {

  title: C
  ext: c
  comment: line: "//"
  module: name: '$$name$$'
  srcfeature: false

  deps: &: {
    kind: *'prod' | string
  }

  # struct is vendored under utility/struct — no external dep. The whole
  # pipeline is synchronous; the transport uses libcurl when available at
  # build time (weak, optional) else returns a transport error.
  deps: {
  }
}


main: kit: feature: &: target: c: deps: &: {
  kind: *'prod' | string
}


# Publication: C has no central registry — publishing IS the git tag.
main: kit: target: c: publish: {
  tag: { active: true }
  registry: {
    state: 'pending'
    active: false
    name: 'none'
    url: ''
    vault: { alias: 'none', env: 'NONE' }
  }
}


# Feature source may be trimmed to the model's selection. The one template
# needing the COMPLETE set is the cross-feature test suite, which calls
# every feature_*_new() — so it is dropped alongside the features it
# exercises whenever the set is trimmed.
main: kit: target: c: feature: {
  trim: true
  fullset: ['tests/feature_test.c']
}
