
main: kit: feature: timeout: {

  name: key()
  title: "Per-request timeout with transport abort"
  version: '0.0.1'
  active: true

  config: {
    options: {
      active: false
      ms: 30000
    }
  }

  # Timeout wraps the transport in init(); it dispatches no pipeline hooks.
  hook: {}
}
