# ProjectName widgets

Generated by `@acme/sdkgen-widgets` for the `wtest` target.

`ProjectName` above is a placeholder: `target add` copies this tree with the
standard replace map applied, so a consumer sees its own SDK name here. A
literal `ProjectName` surviving into generated output means the replace map
did not reach this file — which is the leak `generate.test.ts` scans for.
